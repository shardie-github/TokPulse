services:
  dashboard: { restart: unless-stopped }
  support:   { restart: unless-stopped }
  billing:   { restart: unless-stopped }
  shopify:   { restart: unless-stopped }
  caddy:     { restart: unless-stopped }
